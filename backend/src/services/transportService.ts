import type{ManualReservationInput,TransportRepository}from"../repositories/transportRepository.js";import{ApiError}from"../utils/apiError.js";
export class TransportService{constructor(private readonly repo:TransportRepository){}list(){return this.repo.list();}get(id:string){const value=this.repo.find(id);if(!value)throw new ApiError(404,"RESOURCE_NOT_FOUND","Rimorchio non trovato");return value;}inspection(id:string,date:string|null){const value=this.repo.setInspection(id,date);if(!value)throw new ApiError(404,"RESOURCE_NOT_FOUND","Rimorchio non trovato");return value;}disable(id:string,input:{reason:string;notes?:string}){if(!["Revisione","Manutenzione","Guasto","Altro"].includes(input.reason))throw new ApiError(400,"VALIDATION_ERROR","Motivo non valido");try{const value=this.repo.disable(id,input.reason,input.notes??"");if(!value)throw new ApiError(404,"RESOURCE_NOT_FOUND","Rimorchio non trovato");return value;}catch(error){this.handleConflict(error);}}enable(id:string){try{const value=this.repo.enable(id);if(!value)throw new ApiError(404,"RESOURCE_NOT_FOUND","Rimorchio non trovato");return value;}catch(error){this.handleConflict(error);}}reserve(id:string,input:ManualReservationInput){this.validateReservation(input);try{const value=this.repo.reserve(id,input);if(!value)throw new ApiError(404,"RESOURCE_NOT_FOUND","Rimorchio non trovato");return value;}catch(error){this.handleConflict(error);}}updateReservation(id:string,input:ManualReservationInput){this.validateReservation(input);try{const value=this.repo.updateReservation(id,input);if(!value)throw new ApiError(404,"RESOURCE_NOT_FOUND","Rimorchio non trovato");return value;}catch(error){this.handleConflict(error);}}plannedDeparture(id:string,date:string|null){if(date?.trim()&&!isDateOnly(date.trim()))throw new ApiError(400,"VALIDATION_ERROR","Partenza prevista non valida");try{const value=this.repo.setPlannedDeparture(id,date);if(!value)throw new ApiError(404,"RESOURCE_NOT_FOUND","Rimorchio non trovato");return value;}catch(error){this.handleConflict(error);}}releaseReservation(id:string){try{const value=this.repo.releaseReservation(id);if(!value)throw new ApiError(404,"RESOURCE_NOT_FOUND","Rimorchio non trovato");return value;}catch(error){this.handleConflict(error);}}private validateReservation(input:ManualReservationInput){if(!input.commessa?.trim()||!input.cliente?.trim()||!input.carico?.trim())throw new ApiError(400,"VALIDATION_ERROR","Commessa, cliente e carico sono obbligatori");const planned=input.plannedDepartureDate?.trim();if(planned&&!isDateOnly(planned))throw new ApiError(400,"VALIDATION_ERROR","Partenza prevista non valida");}private handleConflict(error:unknown):never{if(error instanceof ApiError)throw error;if(error instanceof Error&&error.message==="TRAILER_IN_USE")throw new ApiError(409,"RESOURCE_IN_USE","Il rimorchio ha un trasporto attivo");if(error instanceof Error&&error.message==="TRAILER_NOT_AVAILABLE")throw new ApiError(409,"TRAILER_NOT_AVAILABLE","Rimorchio non disponibile");if(error instanceof Error&&error.message==="RESERVATION_NOT_EDITABLE")throw new ApiError(409,"RESERVATION_NOT_EDITABLE","La prenotazione non è modificabile");if(error instanceof Error&&error.message==="PLANNED_DEPARTURE_NOT_EDITABLE")throw new ApiError(409,"PLANNED_DEPARTURE_NOT_EDITABLE","La partenza prevista non è modificabile");throw error;}}
const isDateOnly=(value:string):boolean=>{const match=/^(\d{4})-(\d{2})-(\d{2})$/.exec(value);if(!match)return false;const date=new Date(Date.UTC(Number(match[1]),Number(match[2])-1,Number(match[3])));return date.getUTCFullYear()===Number(match[1])&&date.getUTCMonth()===Number(match[2])-1&&date.getUTCDate()===Number(match[3]);};
