import type{FastifyPluginAsync}from"fastify";import{LoadingController}from"../controllers/loadingController.js";import type{LoadingService}from"../services/loadingService.js";import{idParamsSchema}from"./routeSchemas.js";
interface O{service:LoadingService}const settings={type:"object",additionalProperties:false,required:["operatorId","destinationType"],properties:{operatorId:{type:"string",minLength:1},destinationType:{enum:["RIMORCHIO_ESSEPI","TRASPORTATORE"]},trailerId:{type:"string"},carrierId:{type:"string"}}}as const;
export const loadingRoutes:FastifyPluginAsync<O>=async(app,{service})=>{const c=new LoadingController(service);app.get("/loading-sessions",c.list);app.get("/loads/:loadId/loading-session",c.byLoad);app.post("/loads/:loadId/loading-session",{schema:{body:settings}},c.create);app.patch("/loading-sessions/:id",{schema:{params:idParamsSchema,body:settings}},c.update);app.post("/loading-sessions/:id/units",{schema:{params:idParamsSchema,body:{type:"object",additionalProperties:false,required:["unitType","operatorId"],properties:{unitType:{enum:["PANEL","PACKAGE"]},panelId:{type:"string"},packageId:{type:"string"},operatorId:{type:"string",minLength:1}}}}},c.add);app.delete("/loading-sessions/:id/units/:unitId",{schema:{body:{type:"object",additionalProperties:false,required:["operatorId"],properties:{operatorId:{type:"string",minLength:1}}}}},c.remove);app.post("/loading-sessions/:id/complete",{schema:{params:idParamsSchema}},c.complete);app.post("/loading-sessions/:id/reopen",{schema:{params:idParamsSchema,body:{type:"object",additionalProperties:false,properties:{note:{type:"string"}}}}},c.reopen);app.post("/loading-sessions/:id/ship",{schema:{params:idParamsSchema,body:{type:"object",additionalProperties:false,required:["carrierId"],properties:{carrierId:{type:"string",minLength:1}}}}},c.ship);};
